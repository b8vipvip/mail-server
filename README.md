# mail-server

Infrastructure and administration tooling for the existing Docker Mailserver + Roundcube deployment serving `cn2.io` and `mv3.cn`.

> Production secrets, mailbox databases, DKIM private keys, TLS private keys and real mail data must never be committed.

Development changes are made through pull requests and validated by GitHub Actions before merge.
